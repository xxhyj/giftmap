# 마이그레이션을 올릴 때 쓰는 값. 실제 값은 `.env.local` 에만 두고 커밋하지 않는다.
#
# OpenAI 키는 여기 없다. 추천은 Vercel API 가 하고, 키는 Vercel 환경변수에만 있다.

# Supabase CLI 인증 토큰 (supabase db push 등)
SUPABASE_ACCESS_TOKEN=<supabase access token>
